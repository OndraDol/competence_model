# Copy to .env and fill in real values. Never commit .env.

# Datacruit API
DATACRUIT_USERNAME=
DATACRUIT_PASSWORD=

# Sdílené heslo pro šifrování/dešifrování dashboard dat (AES-256-GCM + PBKDF2).
# Vygenerovat: openssl rand -base64 24
DASHBOARD_PASSWORD=
